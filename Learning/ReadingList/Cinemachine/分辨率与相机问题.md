[设计适应不同屏幕分辨率的UI - 知乎](https://zhuanlan.zhihu.com/p/100842584)

**防止变形**
如果允许图片适当地裁切，保证它不会变形，那么可以给Image添加Aspect Ratio Fitter组件，并且在Aspect Mode中选择Envelope Parent。

![](https://pic4.zhimg.com/80/v2-dc0ec792d5a3acb945d81d499546c727_1440w.webp)

  
将分辨率修改到极端情况，Rect Transform锚点设置的图片虽然显示全了，但是整体变形都被拉伸了。而Aspect Ratio Fitter设置的图片被裁切掉了部分内容，图片整体并没有发生变形。

**任意分辨率下相机视口宽高比保持一致**

对于很多手机游戏，窗口不能随意拉伸，而且在任意分辨率下，游戏视口的宽高比应该保持一致（避免分辨率不同导致的视野大小不一致）。这种情况下就不能简单使用Scale With Screen Size来解决了。

最简单的方法是：

在Canvas下新建一个空物体Panel，所有的UI元素都放到这个Panel下。

在这个物体上加一个组件AspectRatioFitter，Aspect Mode设置为Fit In Parent，Aspect Ratio 设置为UI设计的宽高比。
![[Pasted image 20240104193822.png]]
![[Pasted image 20240104194124.png]]

经过上面3步设置后，UI就会保持宽高比并且保持在屏幕内，但是相机的视口可能会超出该宽高比，需要给相机一个脚本，让相机的视口也能保持宽高比。

```lua
private float aspectRatio = 1.7786f;

    private Camera _camera;
    private float lastAspectRatio;
    private void Start()    {
        _camera = GetComponent<Camera>();
        _camera.aspect = aspectRatio;
    }
    private void Update()    {
        float aspect = Screen.width / (float)Screen.height;
        if (aspect == lastAspectRatio)
        {
            return;
        }
        lastAspectRatio = aspect;
        //当前屏幕比较宽，需要两边留黑        
        if (aspect > lastAspectRatio)
        {
            float actualWidth = Screen.height * aspectRatio;
            float viewportWidth = actualWidth / Screen.width;
            float viewportX = (1 - viewportWidth) / 2;
            _camera.rect = new Rect(viewportX, 0, viewportWidth, 1);
        }
        //当前屏幕比较窄，需要上下留黑        
        else{
            float actualHeight = Screen.width / aspectRatio;
            float viewportHeight = actualHeight / Screen.height;
            float viewportY = (1 - viewportHeight) / 2;
            _camera.rect = new Rect(0, viewportY, 1, viewportHeight);
        }
    }
```


[(99+ 封私信 / 80 条消息) Unity分辨率适配只是针对UI元素吗？ - 知乎](https://www.zhihu.com/question/311818853)


你会发现，3D物体似乎适应了这个窗口，但[UI元素](https://www.zhihu.com/search?q=UI%E5%85%83%E7%B4%A0&search_source=Entity&hybrid_search_source=Entity&hybrid_search_extra=%7B%22sourceType%22%3A%22answer%22%2C%22sourceId%22%3A595906467%7D)却消失了。

那么为什么3D物体会适应这个窗口呢？我们知道，场景是由 Camera 进行渲染的，而我们看到的窗体上的内容，也就是 Camera 看到的内容，而 **Camera 和物体在3D空间中的位置、旋转等都没有改变，所以 Camera 渲染到屏幕上的物体也就很容易的自适应了**，当然，你可以试试增加窗体的宽度而不改变高度看看会发生什么（**你可以直接拖拽Game视图来完成这项实验**）。

  

那么UI元素去哪里呢？**虽然你可以将UI当做3D物体渲染**（Unity版本中很多UI元素也是这么做的，像是NGUI），**但他们出现的位置往往不是你想要的，因此更多的我们将UI元素相对于屏幕或者一个专用的Camera进行渲染。**假如你直接这么进行渲染并且不做一些适配处理的话，你会发现随着窗体宽度的增加，你原本希望出现在左上角的元素慢慢的跑到了中间。

ugui中有一个叫**锚点**的东西，而我们刚才UI元素为什么会消失呢？因为这个UI元素锚点设置为了中心，也就是说，这个元素是相对于中心进行偏移的。而当我们把锚点设置为左上角后，UI元素就会相对于屏幕左上角进行偏移。

  