---
description: 新闻分析项目的问答记录规则
alwaysApply: true
---

# 问答记录规则

- 采用一问一答的方式与用户交互
- 每次问答完成后，将问题和回答记录在当前目录的 `memo.md` 文件中
- 若 `memo.md` 不存在则自动创建，已存在则追加新内容
- 记录格式：以 `## Q{序号}: {问题}` 作为标题，下方为结构化的回答内容
- 在 md 文件开头维护一个"目录"区域，使用 Obsidian wikilink 语法链接到对应标题：
  - 目录链接格式：`- [[#Q{序号}: {标题}]]`
  - 使用 `[[#标题全文]]` 的方式引用同文档内的标题，标题文本需与实际标题完全一致
  - 例如标题 `## Q1: 伊朗局势` 的目录链接为 `- [[#Q1: 伊朗局势]]`
  - 新增问题时同步更新目录
